branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "❌ ERROR: Direct commits to '$branch' are not allowed."
  echo "👉 Please create a feature branch first: git checkout -b feature/your-task"
  exit 1
fi